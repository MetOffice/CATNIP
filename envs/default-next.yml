# Use this file to create a conda environment using:
# conda env create --file envs/default-next.yml

name: default-next
channels:
  - conda-forge
dependencies:
  - python=3.7
  - iris=2.4
  - numpy=1.15
  - cartopy=0.17.0
  - cftime=1.0.0b1
  - mo_pack
  - imagehash
  - pytest
  - flake8
