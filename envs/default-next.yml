# Use this file to create a conda environment using:
# conda env create --file envs/default-next.yml

name: default-next
channels:
  - conda-forge
dependencies:
  - python=3.7
  - iris=2.4
  - numpy=1.15.4
  - scipy=1.3.3
  - cftime=1.0.1
  - pytest
