name: Python Package using Conda

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      #fail-fast: false
      matrix:
        env: [current, next]
    steps:
    - uses: actions/checkout@v2
    #- name: Set up Python
      #uses: actions/setup-python@v2
      #with:
        #channels: conda-forge
        #channel-priority: true
        #activate-environment: anaconda-client-env
        #environment-file: envs/default-${{ matrix.env }}.yml
    - name: checking conda info
      shell: bash
      run: |
        source '/usr/share/miniconda/etc/profile.d/conda.sh'
        conda install -c conda-forge mamba
        mamba env update -q --file envs/default-${{ matrix.env }}.yml --name default-${{ matrix.env }}
    #- name: checking conda list
    #  shell: bash
    #  run: |
    #    source '/usr/share/miniconda/etc/profile.d/conda.sh'
    #    #conda activate default-${{ matrix.env }}
    #    conda list
